name: Build Integrity Check

on:
  push:
  pull_request:

jobs:
  integrity-check:
    runs-on: windows-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '8.0.x'

    - name: Restore Dependencies
      run: dotnet restore

    - name: Build Solution
      run: dotnet build --no-restore

    - name: Run Integrity Analyzer
      run: dotnet run --project BuildIntegrityAnalyzer
      - name: Show Summary
  run: |
    echo "## Build Integrity Report" >> $GITHUB_STEP_SUMMARY
    echo "| Severity | Issue |" >> $GITHUB_STEP_SUMMARY
    echo "|---|---|" >> $GITHUB_STEP_SUMMARY
    cat integrity-report.txt >> $GITHUB_STEP_SUMMARY
